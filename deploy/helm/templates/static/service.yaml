{{- if .Values.mode.app }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "..names.name" . }}-static
  labels:
    {{- include "..labels.standard" . | nindent 4 }}
    app: {{ include "..names.name" . }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.static.service.port }}
      targetPort: {{ .Values.static.service.port }}
  selector:
    {{- include "..labels.matchLabels" . | nindent 4 }}
    app: {{ include "..names.name" . }}-static

{{- end }}