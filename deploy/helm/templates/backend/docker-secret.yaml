{{- if .Values.mode.app }}

apiVersion: v1
kind: Secret
metadata:
  name: {{ include "..names.fullname" . }}-backend-pull-secret
  labels:
    {{- include "..labels.standard" . | nindent 4 }}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{ include "..imagePullSecret" .Values.static.image }}

{{- end}}